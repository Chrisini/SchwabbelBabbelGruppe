#!/usr/bin/env python3

"""Chrisys Text Analyser - Python yeey"""

analyse_file = []


def count(word):
    

""" WHILE ALL FILES"""

if len(sys.argv) < 2:
    print("Provide one or more file(s) as argument!")
    sys.exit()


while TURE:
    file_count = 1
    try:
        f = open(analyse_file[file_count],"r")
        str = f.readline(1024)
        i = 0
        for line in str:
            i+=1
            print("{:04d}".format(i)+':',line.rstrip())
        f.close
    except IOError:
        print("File not found.")
        sys.exit()
    file_count += 1

